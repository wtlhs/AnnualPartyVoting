# 🔧 修复 ERR_SSL_PROTOCOL_ERROR 错误

## 问题描述
访问 `http://192.168.0.97:3000` 时出现 `ERR_SSL_PROTOCOL_ERROR` 错误。

## 原因分析
1. 浏览器尝试使用HTTPS访问HTTP服务
2. 浏览器缓存了之前的HTTPS重定向
3. HSTS (HTTP Strict Transport Security) 策略影响

## 解决方案

### 方案一：清除浏览器缓存（推荐）

#### Chrome浏览器
1. 按 `Ctrl + Shift + Delete` 打开清除浏览器数据
2. 选择"全部时间"
3. 勾选"缓存的图片和文件"和"Cookie及其他网站数据"
4. 点击"清除数据"
5. 重新访问 `http://192.168.0.97:3000`

#### 或者使用隐私模式
1. 按 `Ctrl + Shift + N` 打开隐身窗口
2. 在隐身窗口中访问 `http://192.168.0.97:3000`

### 方案二：手动输入完整HTTP地址
确保在地址栏输入完整的HTTP地址：
```
http://192.168.0.97:3000
```
**注意**：必须包含 `http://` 前缀

### 方案三：禁用浏览器HTTPS重定向
#### Chrome浏览器
1. 在地址栏输入：`chrome://net-internals/#hsts`
2. 在"Delete domain security policies"部分
3. 输入域名：`192.168.0.97`
4. 点击"Delete"
5. 重新访问网站

### 方案四：使用其他端口
如果问题持续，可以使用其他端口：
```bash
# 停止当前服务
Ctrl + C

# 使用8080端口启动
PORT=8080 npm run start:lan
```
然后访问：`http://192.168.0.97:8080`

### 方案五：修改hosts文件（临时方案）
1. 以管理员身份打开记事本
2. 打开文件：`C:\Windows\System32\drivers\etc\hosts`
3. 添加一行：`192.168.0.97 voting.local`
4. 保存文件
5. 访问：`http://voting.local:3000`

## 验证步骤

1. **检查服务器状态**
   ```bash
   # 确认服务器正在运行
   curl http://192.168.0.97:3000
   ```

2. **检查网络连接**
   ```bash
   # 测试网络连通性
   ping 192.168.0.97
   ```

3. **检查端口开放**
   ```bash
   # 测试端口连通性
   telnet 192.168.0.97 3000
   ```

## 移动设备解决方案

### Android设备
1. 打开Chrome浏览器
2. 点击右上角三点菜单
3. 选择"设置" → "隐私设置和安全性" → "清除浏览数据"
4. 选择"全部时间"，清除缓存
5. 重新访问网站

### iOS设备
1. 打开Safari浏览器
2. 点击右下角书签图标
3. 选择"历史记录" → "清除"
4. 选择"全部时间"
5. 重新访问网站

## 预防措施

1. **始终使用HTTP协议**
   - 确保地址栏显示 `http://` 而不是 `https://`

2. **避免浏览器自动重定向**
   - 在地址栏直接输入完整URL
   - 不要使用书签（可能保存了错误的协议）

3. **使用专用浏览器**
   - 为年会投票系统使用专门的浏览器或隐身模式

## 技术支持

如果以上方案都无法解决问题：

1. **检查防火墙设置**
   - 确保Windows防火墙允许Node.js通过
   - 检查企业防火墙设置

2. **尝试其他浏览器**
   - Firefox: `http://192.168.0.97:3000`
   - Edge: `http://192.168.0.97:3000`
   - Safari: `http://192.168.0.97:3000`

3. **联系技术人员**
   - 提供具体的错误截图
   - 说明使用的设备和浏览器版本
   - 描述已尝试的解决方案

---

**快速解决**：99%的情况下，清除浏览器缓存或使用隐身模式可以解决此问题。